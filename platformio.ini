; PlatformIO Project Configuration File
;
;   Build options: build flags, source filter
;   Upload options: custom upload port, speed and extra flags
;   Library options: dependencies, extra library storages
;   Advanced options: extra scripting
;
; Please visit documentation for the other options and examples
; https://docs.platformio.org/page/projectconf.html

[env:esp32-s3-n16r8]
platform = espressif32
board = esp32-s3-devkitc-1
framework = arduino
monitor_speed = 115200
upload_port = COM7
monitor_port = COM7
upload_speed = 921600
board_build.flash_size = 16MB
board_upload.flash_size = 16MB
board_build.partitions = default_16MB.csv
board_build.arduino.memory_type = qio_opi
build_flags = 
	-DARDUINO_USB_CDC_ON_BOOT=0
	-DARDUINO_USB_MODE=1
	-DBOARD_HAS_PSRAM
	-DCORE_DEBUG_LEVEL=3
; BLE stack = Bluedroid (built into Arduino-ESP32 core, no extra lib).
; Firmware uses raw esp_ble_gattc_* IDF APIs to act as a GATT client over
; the existing iPhone->ESP32 link (ANCS). ArduinoJson kept for stage 3 (server POST).
lib_deps =
	bblanchon/ArduinoJson @ ^7.0.0
